class Computers{
  final String processor;
  final String processorGeneration;
  final int ram;
  final int rom;
  final String screenType;
  final int screenSize;
  final int battery;
  final String features;
  final String operationSystem;
  Computers(this.processor, this.processorGeneration, this.ram, this.rom, this.screenType, this.screenSize, this.battery, this.features, this.operationSystem);
}